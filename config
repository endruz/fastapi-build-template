# 服务名
SERVICE_NAME=ai-service
# 版本号
VERSION=v1.0
# 维护者，使用 "," 分割，与邮箱相互对应
MAINTAINER=zhangsan,lisi
# 邮箱，使用 "," 分割，与维护者相互对应
EMAIL=zhangsan@email.com,lisi@email.com
# 输出路径，绝对路径和相对路径均可
# 默认值为当前目录下的 output 文件夹
# 若路径不存在会自动创建
# 若路径存在会先清空文件夹内容再构建输出
OUTPUT_DIR=
